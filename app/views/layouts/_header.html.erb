<div data-magellan-expedition="fixed">
  <dl class="sub-nav">
    <dd data-magellan-arrival="build"><%= link_to "Home", "/"%></dd>
    <% if user_signed_in? %>
      <dd data-magellan-arrival="sign-out"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></dd>
    <% else %>
      <dd data-magellan-arrival="sign-in"><%= link_to "Sign In", new_user_session_path %></dd>
      <dd data-magellan-arrival="sign-up"><%= link_to "Sign Up", new_user_registration_path %></dd>
    <% end %>
		
  </dl>
</div>